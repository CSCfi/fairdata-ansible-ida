[Unit]
Description=Fairdata IDA RabbitMQ MetadataAgent daemon
After=network.target

[Service]
User=apache
Group=apache
WorkingDirectory={{ repo_ida_path }}
ExecStart={{ repo_ida_path }}/venv/bin/python -m agents.metadata.metadata_agent

[Install]
WantedBy=multi-user.target
